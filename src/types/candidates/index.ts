export interface CandidateGraduate {
  universiry: string;
  universiry_specializaton: string;
  universiry_grade: string;
  graduate_start: string;
  graduate_end: string;
  graduate_sertificate: FileList;
}

export interface Candidates {
  id: number;
  text: string;
}
