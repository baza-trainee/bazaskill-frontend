export const shortenLangs = (lang: string) => {
  if (lang === 'English') return 'En';
  if (lang === 'Polish') return 'Pl';
  if (lang === 'German') return 'De';
  if (lang === 'French') return 'Fr';
};
